// src/components/HowItWorks.jsx
export default function HowItWorks() {
  const steps = [
    {
      id: 1,
      title: "Place your order",
      desc: "Choose your package and destination easily through our online platform.",
      icon: "📦",
    },
    {
      id: 2,
      title: "We pick it up",
      desc: "Our team collects your parcel safely and prepares it for delivery.",
      icon: "🚚",
    },
    {
      id: 3,
      title: "We ship securely",
      desc: "Your package travels fast and securely using our trusted partners.",
      icon: "✈️",
    },
    {
      id: 4,
      title: "Delivered to your door",
      desc: "Receive your shipment quickly — no stress, no delays.",
      icon: "🏠",
    },
  ];

  return (
    <section className="bg-white py-20 px-6 md:px-16 text-center">
      <h2 className="text-4xl font-extrabold text-gray-900 mb-12">
        How Flashipping Works
      </h2>
      <div className="grid gap-8 md:grid-cols-4">
        {steps.map((step) => (
          <div
            key={step.id}
            className="bg-white p-6 rounded-2xl shadow-xl hover:shadow-2xl transition-transform hover:-translate-y-2"
          >
            <div className="text-5xl mb-4">{step.icon}</div>
            <h3 className="text-xl font-semibold mb-2 text-gray-800">
              {step.title}
            </h3>
            <p className="text-gray-600 text-sm">{step.desc}</p>
          </div>
        ))}
      </div>
    </section>
  );
}